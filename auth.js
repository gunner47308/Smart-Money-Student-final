document.addEventListener('DOMContentLoaded',()=>{if(SMS.session())location.href='dashboard.html';document.querySelectorAll('.eye').forEach(b=>b.onclick=()=>{let i=b.previousElementSibling;i.type=i.type==='password'?'text':'password'});let l=document.querySelector('#loginForm');if(l)l.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(l)),u=SMS.get('users',[]).find(x=>x.email.toLowerCase()===d.email.toLowerCase()&&x.password===d.password);if(!u)return SMS.toast('อีเมลหรือรหัสผ่านไม่ถูกต้อง');SMS.set('session',{id:u.id});location.href='dashboard.html'};let r=document.querySelector('#registerForm');if(r)r.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(r)),users=SMS.get('users',[]);if(d.password.length<6)return SMS.toast('รหัสผ่านต้องมีอย่างน้อย 6 ตัวอักษร');if(d.password!==d.confirm)return SMS.toast('รหัสผ่านไม่ตรงกัน');if(users.some(u=>u.email.toLowerCase()===d.email.toLowerCase()))return SMS.toast('อีเมลนี้ถูกใช้งานแล้ว');let u={id:Date.now().toString(),name:d.name,email:d.email,password:d.password};users.push(u);SMS.set('users',users);SMS.set('session',{id:u.id});SMS.set('onboarded',true);location.href='dashboard.html'}});
