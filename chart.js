/* Lightweight offline canvas charts used in place of an external dependency. */
SMS.drawPie=(id,data,legend)=>{let c=document.getElementById(id);if(!c)return;let x=c.getContext('2d'),w=c.width=c.clientWidth*2,h=c.height=c.clientHeight*2;x.scale(2,2);w/=2;h/=2;let entries=Object.entries(data).filter(x=>x[1]>0),colors=['#2563eb','#22c55e','#f59e0b','#ef4444','#8b5cf6','#14b8a6'],sum=entries.reduce((a,x)=>a+x[1],0)||1,start=-Math.PI/2;entries.forEach(([k,v],i)=>{let a=v/sum*Math.PI*2;x.beginPath();x.moveTo(w/2,h/2);x.arc(w/2,h/2,67,start,start+a);x.fillStyle=colors[i];x.fill();start+=a});x.beginPath();x.arc(w/2,h/2,36,0,7);x.fillStyle=getComputedStyle(document.body).getPropertyValue('--card');x.fill();if(!entries.length){x.fillStyle='#cbd5e1';x.beginPath();x.arc(w/2,h/2,67,0,7);x.fill()}let l=document.getElementById(legend);if(l)l.innerHTML=entries.map(([k,v],i)=>`<span><i style="background:${colors[i]}"></i>${k} ${SMS.money(v)}</span>`).join('')||'<span>ยังไม่มีรายจ่าย</span>'};SMS.drawBars=(id,values,line=false)=>{let c=document.getElementById(id);if(!c)return;let x=c.getContext('2d'),w=c.width=c.clientWidth*2,h=c.height=c.clientHeight*2;x.scale(2,2);w/=2;h/=2;let max=Math.max(...values,1),gap=w/values.length;if(line){x.strokeStyle='#2563eb';x.lineWidth=3;x.beginPath();values.forEach((v,i)=>{let px=gap*i+gap/2,py=h-20-(v/max)*(h-45);i?x.lineTo(px,py):x.moveTo(px,py)});x.stroke()}else values.forEach((v,i)=>{let bh=(v/max)*(h-32);x.fillStyle='#60a5fa';x.fillRect(gap*i+gap*.25,h-18-bh,gap*.5,bh)})};
